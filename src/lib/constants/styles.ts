export enum Sizes {
	PAGE_MAX_WIDTH = 1440,
	COL_WIDTH = 120,
	FOOTER_HEIGHT = 70,
}

export enum Colors {
	OFF_WHITE = '#F6F3ED',
	VIOLET = '#DDB9FF',
	OCHRE = '#EC9035',
	BLACK = '#000',
	WHITE = '#fff',
}

export enum Fonts {
	MONOSPACE = `"IBM Plex Mono", monospace`,
	SANS_SERIF = `'Helvetica Neue', Arial, sans-serif`,
}
