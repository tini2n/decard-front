import styled from 'styled-components';

import { Link } from 'ui/atoms';

const S = {
	Logo: styled(Link)`
		display: block;
		padding: 22px 0;
		margin: 0 10px;
		border-bottom: 1px solid #000;
	`,
};

export { S };
