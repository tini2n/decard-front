import styled from 'styled-components';

import { Link } from 'ui/atoms';

const S = {
	Logo: styled(Link)`
		display: block;
		padding: 1.5rem 0;
		margin: 0 5px;
		border-bottom: 1px solid #000;
	`,
};

export { S };
